apiVersion: v1
kind: ConfigMap
metadata:
  name: operational-assistant-config
  namespace: operational-assistant
  labels:
    app: operational-assistant
data:
  APP_ENV: "production"
  LOG_LEVEL: "INFO"
  DB_PATH: "/data/operational_assistant.db"
  UVICORN_WORKERS: "2"
