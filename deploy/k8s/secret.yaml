# IMPORTANT: This file is a TEMPLATE only.
# Real secret values are injected by the CI/CD pipeline from GitHub Secrets / AWS Secrets Manager.
# Never commit actual secret values to this file.
apiVersion: v1
kind: Secret
metadata:
  name: operational-assistant-secrets
  namespace: operational-assistant
  labels:
    app: operational-assistant
type: Opaque
stringData:
  # Injected by pipeline: echo -n "$API_KEY" | base64
  API_KEY: "__REPLACED_BY_PIPELINE__"
  # Injected by pipeline from AWS Secrets Manager
  AWS_REGION: "__REPLACED_BY_PIPELINE__"
